# Agentic harness

Kick-off slide lists three names. Use all three, with a clear job each:

- `langgraph_runtime.py` — **primary**. All Sequential / Parallel / Loop graphs.
- `deep_agent.py` — **CDRRootAgent** only (plan, subagents, memory).
- `claude_agent.py` — **one specialist** (fact check or outreach) if Anthropic key exists.
- `agentcore.py` — Bedrock AgentCore runtime/memory flags for the recorded demo.
