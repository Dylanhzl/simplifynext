# demo

- `maya/` seed persona, opportunities, inbox, analytics, empty memory, local places
- `fixtures/run_events.jsonl` offline SSE story for the UI
- `outbox/` gitignored outputs (mail, CDR artifacts)
- `DEMO_SCRIPT.md` 3-minute video beats
