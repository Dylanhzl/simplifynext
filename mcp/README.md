# MCP (P1 + P3)

Port **8085**. Model Context Protocol: agents call tools here instead of ad-hoc APIs.

Scaffold: FastAPI `/mcp/tools` + `/mcp/call`. Replace with FastMCP (`mcp` Python SDK) but keep the same tool names.

See [`../STACK.md`](../STACK.md).
